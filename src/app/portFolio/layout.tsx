const Layout = ({children}) => {
    return (
        <div className="group">
            <h1 className="text-[100px]">Our Works</h1>
            {children}
        </div>
    )
}

export default Layout